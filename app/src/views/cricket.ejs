<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Pakistan Cricket ❤️</title>
    <link rel="icon" type="image/png" href="/favicon.png" />
    <!-- Font -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;800&display=swap" rel="stylesheet">

    <style>
        body {
            margin: 0;
            font-family: "Inter", sans-serif;
            background: #f8fafc;
            color: #1e293b;
        }

        /* ==== HERO SECTION ==== */
        .hero {
            background: linear-gradient(
                rgba(37, 99, 235, 0.10),
                rgba(37, 99, 235, 0.10)
            ),
            url('https://images.pexels.com/photos/165734/pexels-photo-165734.jpeg') 
            center/cover no-repeat;

            height: 60vh;
            display: flex;
            align-items: center;
            justify-content: center;
            text-align: center;
            padding: 2rem;
        }

        .hero h1 {
            font-size: 3rem;
            font-weight: 800;
            color: #0f172a;
            margin-bottom: 0.5rem;
        }

        .hero h1 span {
            color: #2563eb;
        }

        .hero h2 {
            font-size: 1.4rem;
            color: #475569;
            font-weight: 500;
        }

        /* ==== CARD ==== */
        .content {
            max-width: 900px;
            margin: -60px auto 40px;
            padding: 0 1rem;
        }

        .card {
            background: white;
            border-radius: 20px;
            padding: 2rem;
            box-shadow: 0 10px 40px rgba(0,0,0,0.08);
            text-align: center;
            animation: fadeIn 0.7s ease;
        }

        .card p {
            font-size: 1.1rem;
            color: #475569;
            line-height: 1.6;
        }

        /* ==== BUTTON ==== */
        .btn {
            margin-top: 25px;
            display: inline-block;
            padding: 12px 26px;
            background: #2563eb;
            color: white;
            font-weight: 600;
            text-decoration: none;
            border-radius: 10px;
            transition: 0.3s ease;
        }

        .btn:hover {
            background: #1d4ed8;
            transform: translateY(-2px);
            box-shadow: 0 8px 18px rgba(37, 99, 235, 0.25);
        }

        /* Animations */
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(30px); }
            to { opacity: 1; transform: translateY(0); }
        }
    </style>
</head>

<body>
<%- include("partials/header", { title: "Welcome to NodeAuth" }) %>

    <!-- HERO -->
    <section class="hero">
        <div>
            <h1>Pakistan Cricket <span>❤️</span></h1>
            <h2>United • Fearless • Passionate</h2>
        </div>
    </section>

    <!-- CARD SECTION -->
    <div class="content">
        <div class="card">
            <p>
                Celebrating the never-ending passion, unity, and unstoppable spirit
                of the Pakistan cricket team and its millions of fans.
            </p>

            <a href="/" class="btn">Back to Home</a>
        </div>
    </div>
<%- include("partials/footer") %>

</body>
</html>
